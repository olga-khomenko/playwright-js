{
  "name": "playwright-tests",
  "version": "1.0.0",
  "description": "Developing skills with palywright",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "test:chrome": "npx playwright test --config=tests/playwright.config.js --project=Chrome",
    "test:firefox":"npx playwright test --config=tests/playwright.config.js --project=Firefox",
    "test:webkit":"npx playwright test --config=tests/playwright.config.js --project=Webkit"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.32.3"
  }
}
